# Optional: broader earnings coverage (FMP primary, Nasdaq fallback)
FMP_API_KEY=

# Auth (GitHub + Neon) — required for live Sign in
# Generate AUTH_SECRET with: openssl rand -base64 32
DATABASE_URL=
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Recommended in production (Vercel + custom domain)
AUTH_URL=https://www.gotconviction.com
AUTH_TRUST_HOST=true
